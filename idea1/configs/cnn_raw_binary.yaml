defaults: base.yaml
embedding:
  use_raw: true
  use_phase: false
  use_rhie: false
  use_gram: false
  freqs: [1, 2, 4]
  include_magnitude: true
model:
  name: simple_cnn
  in_channels: 1
  hidden_dim: 128

